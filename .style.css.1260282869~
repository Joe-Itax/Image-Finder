* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  /* box-shadow: 0 0 0 1px red inset; */
}

:root {
  --bgc: #f1f1f1;
}

body {
  background: var(--bgc);
  font-family: 'Merriweather', serif;
  letter-spacing: 0.5rem;
  line-height: 3rem;
  position: relative;
  /* overflow: hidden; */
}

main {
  /* display: none; */
  /* filter: brightness(10%); */
}

main .welcom {
  background-image: url('./bcgImage.jpeg');
  background-repeat: no-repeat;
  background-position: center;
  background-size: cover;
  height: 100vh;
  width: 100%;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
}

main .welcom .content-welcom {
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  width: 75%;
  padding: 2rem;
}

main .welcom h1 {
  color: #fff;
  font-size: 3rem;
  letter-spacing: 0.2rem;
  line-height: 5rem;
  align-self: start;
  font-family: 'Roboto', sans-serif;
}

main .welcom div.achroche-text p {
  color: #fff;
  font-size: 2rem;
  padding: 2rem 0;
}

main .welcom div.achroche-text p a {
  color: #e0e0e0;
  font-weight: 900;
}

main .welcom .search-box {
  width: 100%;
  /* border: 1px solid blue; */
  display: flex;
}

main .welcom input {
  width: 100%;
  padding: 1rem 2rem;
  padding-right: 5rem;
  font-size: 1.8rem;
  outline: none;
  position: relative;
}

main .welcom input:focus {
  outline: dashed aliceblue;
}

main .welcom button {
  position: absolute;
  padding: 1rem;
  right: 17%;
  margin-top: 2px;
  border: none;
  border-left: 1px solid #000;
  background: transparent;
  cursor: pointer;
}

main .welcom button i {
  font-size: 1.9rem;
}

main .choice-img-video {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  align-items: center;
  gap: 2rem;
  margin: 3rem;
}

main .choice-img-video * {
  margin-bottom: 2rem;
}

main .choice-img-video a {
  text-decoration: none;
  padding: 1rem 2rem;
  border-radius: 4rem;
  border: 1px solid #000;
  color: #555;
  font-size: 1.5rem;
  font-weight: bold;
  transition: all 0.2s;
}

main .choice-img-video a:hover {
  color: #000;
}

main .choice-img-video a.active {
  color: #fff;
  background-color: #000;
  border: 1px solid #000;
  transition: all 0.2s;
}

main .choice-img-video a.active:hover {
  filter: brightness(90%);
}

main .title-of-research-box {
  align-self: start;
  padding: 2rem;
}

main .title-of-research-box h2 {
  font-size: 1.8rem;
  display: none;
}

main .content-box {
  width: 100%;
  height: 100%;
  /* display: flex;
  flex-wrap: wrap;
  justify-content: center;
  align-items: center;
  padding: 1rem; */
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  grid-template-rows: 100%;
}

main .content-box .img-box {
  grid-column: 1 / span 3;
  width: 100%;
  height: 100%;
  /* display: flex;
  flex-wrap: wrap;
  flex-direction: column;
  justify-content: center;
  align-items: center; */
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  grid-template-rows: 100%;
  /* gap: 2rem; */
}

main .content-box .img-box .image {
  /* max-width: 19rem; */
  width: 100%;
  /* columns: 3; */
  /* grid-column: 1 / span 3; */
  grid-column: auto;
}

main .content-box .img-box .image figure {
  max-width: 100%;
  max-height: 100%;
  position: relative;
}

main .content-box .img-box figure img, main .content-box .img-box figure img {
    height: auto;
  width: 100%;
  object-fit: cover;
  height: 80%;
  width: 100%;
  cursor: pointer;
  transition: all 0.2s ease-in-out;
}

main .content-box .img-box figure img:hover,
figure a:hover ~ figure img {
  filter: brightness(80%);
}

figcaption {
  position: absolute;
  left: 0rem; 
  padding: 0;
  width: 100%;
  /* height: 1rem; */
  bottom: 1.4rem;
  /* visibility: hidden; */
  transition: all 0.2s ease-in-out;
  border: 1px solid red;
  display: flex;
  align-items: center;
  justify-content: space-between;
} 

figcaption .link-photographer{
display: flex;
  align-items: center;
  justify-content: space-between;
  border: 1px solid #fff;
}

figcaption .btn-download-icon{
  border: 1px solid blue;
  display: inline-flex;
  align-items: center;
  justify-content: center;
}

figcaption .btn-download-icon i{
  display: block;
  align-self: center;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  color: red;
  background-color: var(--bgc);
  height: 2rem;
  margin: auto;
}

/* main .content-box .img-box figure img:hover ~ figcaption,
figure a:hover ~ figcaption {
  visibility: visible;
} */

figcaption i {
  color: #fff;
  font-size: 1.5rem;
}

figure a {
  /* position: absolute;
  left: 2rem;
  bottom: 1.5rem; */
  text-decoration: none;
  font-size: 1.5rem;
  font-weight: 700;
  letter-spacing: 0.2rem;
  line-height: 2rem;
  color: #fff;
  z-index: 99;
  transition: all 0.2s ease-in-out;
}

main .content-box .img-box figure:hover a {
  visibility: visible;
}

figure a:hover {
  text-decoration: underline;
}

/* Media for responsivity */
@media (max-width: 600px) {
  main .welcom .content-welcom {
    padding: 2rem;
    width: 100%;
  }

  h1 {
    order: 1;
  }

  .achroche-text {
    order: 1;
  }

  main .title-of-research-box {
    order: -1;
  }

  main .content-box {
    padding: 0;
  }

  main .content-box .img-box .image {
    max-width: 100%;
  }

  .search-box {
    margin-top: -2rem;
    margin-bottom: 2rem;
  }

  main .welcom input {
    padding: 1rem 4rem 1rem 0.4rem;
  }

  main .welcom button {
    padding: 1rem;
    right: 7%;
  }
}

@media (max-width: 450px) {
  body {
    line-height: 2.3rem;
  }

  main .welcom h1 {
    font-size: 2.3rem;
    line-height: 3rem;
  }

  main .welcom div.achroche-text p {
    font-size: 1.4rem;
    padding: 1rem 0;
  }
}

.overlay {
  height: 100vh;
  width: 100%;
  position: fixed;
  top: 0;
  /* left: -100%; */
  left: 0%;
  margin: auto;
  /* background-color: #000; */
  background-color: transparent;
  z-index: 1;
  backdrop-filter: blur(5px);
  -webkit-backdrop-filter: blur(5px);
  display: flex;
  align-items: center;
  justify-content: center;
  /* border: yellow 10px solid; */
  display: none;
  overflow: hidden;
}

.overlay figure {
  height: 100vh;
  width: 80%;
  border: red 1px solid;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--bgc);
}

.overlay figure img {
  width: 100%;
  height: auto;
  border: #fff 1px solid;
}

.overlay figure figcaption {
  font-size: 8rem;
  color: #e0e0e0;
}
